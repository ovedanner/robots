<div class="d-flex justify-content-center play">
  <div class="d-flex flex-column">
    <div class="gray-borders mb-2 p-2 header align-items-center justify-content-center">
      <h3>{{model.name}}</h3>
    </div>
    <div class="d-flex flex-row flex-grow-1">
      {{board-multiplayer room=model user=session.user}}
    </div>
  </div>
</div>