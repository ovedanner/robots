<div class="container-fluid d-flex justify-content-center play">
  <div class="d-flex flex-column">
    <div class="blue-borders mb-2 p-2 header align-items-center justify-content-center">
      <h3>Room: {{model.name}}</h3>
    </div>
    <div class="d-flex flex-row">
      {{board-multiplayer room=model user=session.user}}
      {{game-log room=model user=session.user}}
    </div>
  </div>
</div>