{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div id="content-wrapper" class="d-flex">
  {{#if session.isAuthenticated}}
    <div {{action "logout"}} class="logout"><span>Logout</span></div>
  {{/if}}
  <div id="content">
    {{outlet}}
  </div>
</div>